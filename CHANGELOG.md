# Changelog

ประวัติการเปลี่ยนแปลงและการพัฒนาโครงการ TDC e-Asset ระบบบริหารจัดการครุภัณฑ์และสินทรัพย์ไอทีสำหรับหน่วยงานราชการ

การจัดทำเอกสารนี้อ้างอิงตามมาตรฐาน [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) และใช้ระบบระบุเวอร์ชันแบบ [Semantic Versioning](https://semver.org/spec/v2.0.0.html)

---

## [0.1.0] - 2026-09-02

### Added
- สถาปัตยกรรมระบบฐานข้อมูลเริ่มต้นบน Supabase PostgreSQL พร้อมเปิดใช้งาน Row Level Security (RLS) และฟังก์ชันคำนวณจำนวนคงเหลือแบบ Concurrency-Safe ผ่าน Stored Procedure (`borrow_asset_rpc`, `return_asset_rpc`)
- ระบบยืนยันตัวตนแบบไร้รหัสผ่าน (Passwordless Authentication) ผ่าน Magic Link และรหัส OTP 6 หลักทางอีเมล ร่วมกับ Resend Custom SMTP
- ระบบป้องกันการเข้าถึงเส้นทางใช้งาน (Edge Route Guard Middleware) สำหรับเส้นทาง `/dashboard`, `/assets`, `/borrow-return`, `/categories`, `/reports`, `/admin`
- หน้ารวมทะเบียนครุภัณฑ์ (`/assets`) รองรับการค้นหา กรองหมวดหมู่ กรองสถานะ และแสดงผลแบ่งหน้า (Pagination)
- หน้าเพิ่มและแก้ไขข้อมูลครุภัณฑ์ (`/assets/new`, `/assets/[id]/edit`) พร้อมระบบสุ่มสร้างรหัสครุภัณฑ์อัตโนมัติ และเลือกหมวดหมู่ตามมาตรฐานราชการ
- หน้าแสดงรายละเอียดครุภัณฑ์ (`/assets/[id]`) พร้อมไทม์ไลน์ประวัติการยืมคืนในอดีต และหน้าต่างพิมพ์สติกเกอร์บาร์โค้ด Code 128 และ QR Code สำหรับติดตัวเครื่อง
- ระบบจัดการหมวดหมู่ครุภัณฑ์ (`/categories`) พร้อมกล่องสถิติ ตรวจสอบความปลอดภัยก่อนลบ และรองรับการย้ายครุภัณฑ์ไปยังหมวดหมู่อื่นก่อนลบ (Smart Reassign Strategy)
- สถานะครุภัณฑ์ "สูญหาย" (Lost Status) ครอบคลุมทั้งฐานข้อมูล Zod Validation ตัวกรองหน้ารวม ป้ายสถานะ และขั้นตอนการรับคืนพัสดุ
- ระบบรายงานและหน้าต่างพรีวิวเอกสารขนาด A4 (`/reports`) ที่แสดงผลด้วย HTML และ CSS ตามสัดส่วนมาตรฐาน พร้อมแผงตั้งค่า ปรับแต่งมุมมองการพิมพ์ และการดาวน์โหลดเป็น PDF หรือส่งออกเป็นไฟล์ CSV (Excel)
- ระบบอัปโหลดรูปภาพครุภัณฑ์ผ่าน Supabase Storage (Bucket: `asset-images`) แบบ Drag-and-Drop พร้อมระบบแสดงภาพตัวอย่างและปุ่มเปลี่ยนรูป
- สคริปต์รัน Migration อัตโนมัติ (`npm run db:migrate`) ผ่านคำสั่ง Terminal พร้อมตารางบันทึกประวัติ `_schema_migrations` เพื่อป้องกันการรันซ้ำ

### Changed
- ปรับโครงสร้างชุดสีและการออกแบบของระบบให้สอดคล้องกับมาตรฐาน Editorial Design (Warm Ivory, Dark Charcoal Ink, Terracotta Red, Forest Green)
- ปรับปรุงการตรวจสอบสิทธิ์การลบหมวดหมู่ ให้บล็อกการลบหากยังมีครุภัณฑ์ผูกอยู่ เพื่อรักษา Referential Integrity ของฐานข้อมูล
- ปรับเปลี่ยนฟอร์มรูปภาพครุภัณฑ์จากช่องกรอก URL ภายนอก ให้เป็นระบบอัปโหลดไฟล์ตรงเข้า Storage ของหน่วยงาน

### Removed
- นำปุ่มกระดิ่งแจ้งเตือนที่ยังไม่มีฟังก์ชันรองรับออกจากแถบด้านบนของระบบ (Header) เพื่อความสะอาดตาและความถูกต้องในการใช้งาน

### Security
- กำหนดค่า Row Level Security (RLS) บนทุกตารางของ Supabase เพื่อให้เฉพาะผู้ใช้ที่ผ่านการยืนยันตัวตนเท่านั้นที่เข้าถึงข้อมูลได้
- ป้องกันสภาวะแย่งยืมพร้อมกัน (Race Conditions) ในระบบยืมคืน ด้วยการใช้ `FOR UPDATE` Row-Level Lock ภายใน PostgreSQL Transactions
- ซ่อนกุญแจสำคัญ (`SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`) ไว้ฝั่งเซิร์ฟเวอร์ ไม่ส่งออกไปยัง Client-side Browser
